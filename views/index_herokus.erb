<div class="container">

  Heroku-apps<br>
  @heroku_api:<%= @heroku_api.inspect %><br><br><br>
  @apps:<%= @apps.inspect %><br>


  <table class="table table-striped table-bordered">
    <tr>
      <th>id</th>
      <th>app name</th>
    </tr>

    <% @apps.each do |app| %>
      <tr>
        <td><%= app['id'] %></td>
        <td><%= app['name'] %></td>
      </tr>
    <% end %>
  </table>

</div>
